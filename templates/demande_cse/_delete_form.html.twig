<form method="post" action="{{ path('demandecse_delete', {'id': demande_c_s_e.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette demande ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_c_s_e.id) }}">
    <button class="btn">Supprimer</button>
</form>
