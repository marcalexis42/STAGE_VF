<form method="post" action="{{ path('demande_comptable_delete', {'id': demande_comptable.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette demande ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_comptable.id) }}">
    <button class="btn">Supprimer</button>
</form>
